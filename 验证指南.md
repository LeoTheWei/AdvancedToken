# Etherscan 合约验证指南

## ✅ 已修复的问题

扁平化文件 `flattened_fixed.sol` 已经修复：
- ✅ 删除了所有多余的 `pragma solidity` 声明
- ✅ 现在只有一个 `pragma solidity ^0.8.28;` 在文件开头
- ✅ 添加了 SPDX License Identifier

## 📋 验证步骤

### 方法 1：使用 Hardhat 自动验证（强烈推荐）

这是最简单且最可靠的方法：

```bash
npx hardhat run scripts/verify.js --network sepolia
```

**优点**：
- 自动处理所有设置
- 自动编码构造函数参数
- 自动匹配编译器版本和优化设置

### 方法 2：手动在 Etherscan 验证

如果必须手动验证，请按照以下步骤：

#### 步骤 1：获取合约信息

从部署信息文件中获取：
- **合约地址**：`0x32621E05c42A5B03Eec24892d9Dafe42b2852e54`
- **构造函数参数**：`1000000 * 10^18` = `1000000000000000000000000` (hex: `0xd3c21bcecceda1000000`)

#### 步骤 2：访问 Etherscan 验证页面

打开：
```
https://sepolia.etherscan.io/address/0x32621E05c42A5B03Eec24892d9Dafe42b2852e54#code
```

点击 **"Contract"** 标签，然后点击 **"Verify and Publish"**

#### 步骤 3：填写验证信息

**Compiler Type**：选择 `Solidity (Single file)`

**Compiler Version**：选择 `v0.8.28+commit.xxx` 
- 必须选择与部署时完全相同的版本
- 如果不知道确切版本，可以查看编译输出或使用 `v0.8.28` 的最新 commit

**Open Source License Type**：选择 `MIT License (MIT)`

**Optimization**：选择 `Yes`
- **Runs**: `200`
- 这必须与 `hardhat.config.js` 中的设置完全一致

**Enter the Solidity Contract Code below**：
- 复制 `flattened_fixed.sol` 文件的**全部内容**
- 确保从第一行到最后一行都复制

**Constructor Arguments ABI-encoded**：
- 需要编码构造函数参数 `initialSupply`
- 值为：`1000000 * 10^18` = `1000000000000000000000000`

**如何获取编码后的参数**：

方法 A：使用 Hardhat Console
```bash
npx hardhat console --network sepolia
```
然后：
```javascript
const { ethers } = require("hardhat");
const iface = new ethers.Interface(["constructor(uint256 initialSupply)"]);
const encoded = iface.encodeDeploy([ethers.parseUnits("1000000", 18)]);
console.log(encoded.slice(2)); // 去掉 0x 前缀
```

方法 B：使用在线工具
- 访问：https://abi.hashex.org/
- 函数签名：`constructor(uint256)`
- 参数值：`1000000000000000000000000`
- 获取编码后的 hex 字符串

方法 C：直接使用这个值（如果 initialSupply 是 1000000 * 10^18）：
```
00000000000000000000000000000000000000000000000d3c21bcecceda1000000
```

#### 步骤 4：提交验证

点击 **"Verify and Publish"** 按钮

## 🔍 常见问题排查

### 问题 1：仍然出现字节码不匹配

**检查清单**：
1. ✅ 编译器版本必须是 `0.8.28`（精确版本）
2. ✅ 优化设置必须是 `Yes, Runs: 200`
3. ✅ 构造函数参数编码必须正确
4. ✅ 源代码必须完整（没有截断）

**解决方案**：
- 尝试使用 Hardhat 自动验证（最可靠）
- 检查 `artifacts/build-info/` 中的编译信息，确认编译器版本

### 问题 2：找不到构造函数参数

**获取方法**：
```bash
# 查看部署脚本
cat scripts/deploy.js | grep initialSupply

# 或使用 Hardhat 获取编码参数
npx hardhat verify --network sepolia \
  0x32621E05c42A5B03Eec24892d9Dafe42b2852e54 \
  1000000000000000000000000
```

### 问题 3：编译器版本不匹配

**解决方法**：
1. 查看编译输出确认版本：
   ```bash
   npx hardhat compile
   # 查看输出中的编译器版本
   ```

2. 或者在 Etherscan 上尝试不同的 0.8.28 commit 版本

## 📝 验证检查清单

在提交验证前，确保：

- [ ] 使用了修复后的 `flattened_fixed.sol` 文件
- [ ] 编译器版本：`v0.8.28+commit.xxx`
- [ ] 优化设置：`Yes, Runs: 200`
- [ ] SPDX License：`MIT License (MIT)`
- [ ] 构造函数参数已正确编码
- [ ] 源代码完整（检查文件大小）

## 🎯 推荐方案

**强烈建议使用 Hardhat 自动验证**：

```bash
# 1. 确保环境变量已设置
# .env 文件包含：
# ETHERSCAN_API_KEY=your_api_key
# SEPOLIA_RPC_URL=your_rpc_url

# 2. 运行验证
npx hardhat run scripts/verify.js --network sepolia
```

这样可以避免所有手动验证的问题！

## 📞 如果仍然失败

如果使用 Hardhat 自动验证仍然失败，请检查：

1. **环境变量**：确保 `.env` 文件中有正确的 `ETHERSCAN_API_KEY`
2. **网络连接**：确保可以访问 Sepolia 网络
3. **合约地址**：确保合约地址正确
4. **构造函数参数**：检查 `scripts/verify.js` 中的参数是否正确

---

**提示**：如果手动验证仍然失败，建议使用 Hardhat 自动验证，它会更可靠。

